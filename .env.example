# GitHub OAuth Configuration for BU Scholar
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# GitHub OAuth App Credentials (get from https://github.com/settings/developers)
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Callback URL for OAuth flow
# Development: http://localhost:3000/auth/callback
# Production: https://your-domain.com/auth/callback
GITHUB_REDIRECT_URI=http://localhost:3000/auth/callback

# Notes:
# 1. Create separate GitHub OAuth Apps for development and production
# 2. Make sure the redirect URI in this file matches the one in your GitHub OAuth App
# 3. For production, use HTTPS URLs
# 4. Add .env to .gitignore to prevent committing credentials